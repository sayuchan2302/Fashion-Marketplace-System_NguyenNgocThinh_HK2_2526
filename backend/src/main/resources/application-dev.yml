spring:
  jpa:
    hibernate:
      ddl-auto: ${JPA_DDL_AUTO:update}

app:
  seed:
    enabled: ${APP_SEED_ENABLED:false}

logging:
  level:
    vn.edu.hcmuaf.fit.marketplace: DEBUG
    org.hibernate.SQL: DEBUG
